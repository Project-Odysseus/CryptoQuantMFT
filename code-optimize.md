# Code optimization / revision register

This file tracks code that should be improved later without changing behavior right now.

| File | Area | Reason |
|---|---|---|
| _None currently logged_ | — | The previously logged runtime-symbol, account-state, JSON-persistence, and parquet-sync issues have been addressed. Add new items here only when we explicitly defer a real code-quality issue. |
